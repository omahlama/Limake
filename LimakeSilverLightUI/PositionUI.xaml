<UserControl x:Class="LimakeSilverLightUI.PositionUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
    d:DesignHeight="50" d:DesignWidth="50">
    <Canvas Height="50" Name="canvas" Width="50">
        <Ellipse Canvas.Left="0" Canvas.Top="0" Height="50" Name="ellipse1" Stroke="{Binding NoticeBrush}" StrokeThickness="3" Width="50" Fill="{Binding FillBrush}" />
        <TextBlock Canvas.Left="0" Canvas.Top="-10" Height="60" Width="50" TextAlignment="Center" Name="CountBox" Text="" FontSize="48" FontFamily="Comic Sans MS"/>
    </Canvas>
</UserControl>
